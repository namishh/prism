local colors = require("prism.themer"):getColors()
local utils = require("prism.utils")

return {
  HarpoonWindow = { fg = colors.black, bg = colors.color12 },
  HarpoonBorder = { fg = colors.color4 },
}
